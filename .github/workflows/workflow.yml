name: Update

on:
  push:
    branches:
      - master

  schedule:
    - cron: '0 2 * * *'

  pull_request:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Test updater helpers
        run: |
          ./tests/test_edge_alpine_updater.sh
          ./tests/test_gotpl_updater.sh
  tooling:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        script: [ gotpl ]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions
        with:
          dir: repos
          script: ${{ matrix.script }}
          docker_username: ${{ github.event_name != 'pull_request' && secrets.DOCKER_USERNAME || '' }}
          docker_password: ${{ github.event_name != 'pull_request' && secrets.DOCKER_PASSWORD || '' }}
          wodbot_github_pat: ${{ github.event_name == 'pull_request' && github.token || secrets.WODBOT_GITHUB_PAT }}
          wodbot_github_username: ${{ github.event_name == 'pull_request' && 'x-access-token' || secrets.WODBOT_GITHUB_USERNAME }}
          wodbot_git_email: ${{ github.event_name == 'pull_request' && 'ci@wodby.invalid' || secrets.WODBOT_GIT_EMAIL }}
          wodbot_git_name: ${{ github.event_name == 'pull_request' && 'Wodby CI' || secrets.WODBOT_GIT_NAME }}
  alpine:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        script: [ mariadb,nginx,opensmtpd,vinyl,squid ]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions
        with:
          dir: alpine
          script: ${{ matrix.script }}
          docker_username: ${{ github.event_name != 'pull_request' && secrets.DOCKER_USERNAME || '' }}
          docker_password: ${{ github.event_name != 'pull_request' && secrets.DOCKER_PASSWORD || '' }}
          wodbot_github_pat: ${{ github.event_name == 'pull_request' && github.token || secrets.WODBOT_GITHUB_PAT }}
          wodbot_github_username: ${{ github.event_name == 'pull_request' && 'x-access-token' || secrets.WODBOT_GITHUB_USERNAME }}
          wodbot_git_email: ${{ github.event_name == 'pull_request' && 'ci@wodby.invalid' || secrets.WODBOT_GIT_EMAIL }}
          wodbot_git_name: ${{ github.event_name == 'pull_request' && 'Wodby CI' || secrets.WODBOT_GIT_NAME }}
  images:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        script: [ alpine,apache,memcached,node,php,postgres,python,go,redis,valkey,ruby,rabbitmq ]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions
        with:
          dir: images
          script: ${{ matrix.script }}
          docker_username: ${{ github.event_name != 'pull_request' && secrets.DOCKER_USERNAME || '' }}
          docker_password: ${{ github.event_name != 'pull_request' && secrets.DOCKER_PASSWORD || '' }}
          wodbot_github_pat: ${{ github.event_name == 'pull_request' && github.token || secrets.WODBOT_GITHUB_PAT }}
          wodbot_github_username: ${{ github.event_name == 'pull_request' && 'x-access-token' || secrets.WODBOT_GITHUB_USERNAME }}
          wodbot_git_email: ${{ github.event_name == 'pull_request' && 'ci@wodby.invalid' || secrets.WODBOT_GIT_EMAIL }}
          wodbot_git_name: ${{ github.event_name == 'pull_request' && 'Wodby CI' || secrets.WODBOT_GIT_NAME }}
  stability-tags:
    needs: tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        script: [ drupal-php,wordpress-php,matomo-php,webgrind-php,laravel-php,drupal,wordpress,xhprof,drupal-cms,edge-alpine ]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions
        with:
          dir: stability-tags
          script: ${{ matrix.script }}
          docker_username: ${{ github.event_name != 'pull_request' && secrets.DOCKER_USERNAME || '' }}
          docker_password: ${{ github.event_name != 'pull_request' && secrets.DOCKER_PASSWORD || '' }}
          wodbot_github_pat: ${{ github.event_name == 'pull_request' && github.token || secrets.WODBOT_GITHUB_PAT }}
          wodbot_github_username: ${{ github.event_name == 'pull_request' && 'x-access-token' || secrets.WODBOT_GITHUB_USERNAME }}
          wodbot_git_email: ${{ github.event_name == 'pull_request' && 'ci@wodby.invalid' || secrets.WODBOT_GIT_EMAIL }}
          wodbot_git_name: ${{ github.event_name == 'pull_request' && 'Wodby CI' || secrets.WODBOT_GIT_NAME }}
  upstream:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        script: [ adminer,cachet,drupal,mariadb,matomo,nginx,webgrind,wordpress,xhprof,drupal-cms,solr,zookeeper,openclaw,prometheus,vinyl ]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions
        with:
          dir: upstream
          script: ${{ matrix.script }}
          docker_username: ${{ github.event_name != 'pull_request' && secrets.DOCKER_USERNAME || '' }}
          docker_password: ${{ github.event_name != 'pull_request' && secrets.DOCKER_PASSWORD || '' }}
          wodbot_github_pat: ${{ github.event_name == 'pull_request' && github.token || secrets.WODBOT_GITHUB_PAT }}
          wodbot_github_username: ${{ github.event_name == 'pull_request' && 'x-access-token' || secrets.WODBOT_GITHUB_USERNAME }}
          wodbot_git_email: ${{ github.event_name == 'pull_request' && 'ci@wodby.invalid' || secrets.WODBOT_GIT_EMAIL }}
          wodbot_git_name: ${{ github.event_name == 'pull_request' && 'Wodby CI' || secrets.WODBOT_GIT_NAME }}
  docker4x:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        script: [ drupal,php,python,ruby,wordpress,laravel ]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions
        with:
          dir: docker4x
          script: ${{ matrix.script }}
          docker_username: ${{ github.event_name != 'pull_request' && secrets.DOCKER_USERNAME || '' }}
          docker_password: ${{ github.event_name != 'pull_request' && secrets.DOCKER_PASSWORD || '' }}
          wodbot_github_pat: ${{ github.event_name == 'pull_request' && github.token || secrets.WODBOT_GITHUB_PAT }}
          wodbot_github_username: ${{ github.event_name == 'pull_request' && 'x-access-token' || secrets.WODBOT_GITHUB_USERNAME }}
          wodbot_git_email: ${{ github.event_name == 'pull_request' && 'ci@wodby.invalid' || secrets.WODBOT_GIT_EMAIL }}
          wodbot_git_name: ${{ github.event_name == 'pull_request' && 'Wodby CI' || secrets.WODBOT_GIT_NAME }}
  notify:
    name: Notify Report Events
    needs:
      - tests
      - tooling
      - alpine
      - images
      - stability-tags
      - upstream
      - docker4x
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Download Report Artifacts
        id: download-reports
        uses: actions/download-artifact@v8
        continue-on-error: true
        with:
          path: reports

      - name: Generate Image Update Report
        run: |
          python scripts/image_update_report.py \
            --readme README.md \
            --events-dir reports \
            --output-dir image-update-report

      - name: Upload Consolidated Report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: image-update-report
          path: image-update-report
          if-no-files-found: warn
          retention-days: 14

      - name: Send Email Digest
        if: github.event_name != 'pull_request'
        env:
          REPORT_EMAIL_TO: ${{ secrets.REPORT_EMAIL_TO }}
          REPORT_EMAIL_FROM: ${{ secrets.REPORT_EMAIL_FROM }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_SSL: ${{ secrets.SMTP_SSL }}
          SMTP_STARTTLS: ${{ secrets.SMTP_STARTTLS }}
        run: |
          python scripts/image_report_email.py image-update-report \
            --run-url "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            --event "${{ github.event_name }}" \
            --sha "${{ github.sha }}" \
            --workflow-result "tests=${{ needs.tests.result }}, tooling=${{ needs.tooling.result }}, alpine=${{ needs.alpine.result }}, images=${{ needs.images.result }}, stability-tags=${{ needs['stability-tags'].result }}, upstream=${{ needs.upstream.result }}, docker4x=${{ needs.docker4x.result }}" \
            --artifact-result "${{ steps.download-reports.outcome }}"
